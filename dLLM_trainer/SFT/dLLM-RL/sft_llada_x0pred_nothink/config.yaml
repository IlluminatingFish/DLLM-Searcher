wandb:
  entity: mz751-rutgers-university
  resume: auto
  run_id: llada_sft_x0pred_nothink
experiment:
  project: sft_llada_x0pred_nothink
  num_node: 1
  resume_from_checkpoint: null
  logging_dir: sft_llada_x0pred_nothink/logs
model:
  pretrained_model: /common/users/mz751/Projects/dLLM_trainer/checkpoints/base/llada_model
  optimized_name: optimized
dataset:
  optimization_data: sft_train_final
  strip_think: true
training:
  resume_epoch: null
  gradient_checkpointing_enable: false
  gradient_accumulation_steps: 4
  batch_size_lm: 1
  mixed_precision: bf16
  enable_tf32: true
  seed: 10086
  num_train_epochs: 10
  max_grad_norm: 1.0
  method: semi-ar
  lower_p: 0.1
  upper_p: 0.9
  block_size: 64
  post_num: 0
  max_gen_length: 4096
  max_prompt_len: 2048
  lambda_c: 0.1
  sigma_min: 0.1
  sigma_max: 1.0
optimizer:
  name: adamw
  params:
    learning_rate: 1.0e-05
    scale_lr: false
    beta1: 0.9
    beta2: 0.999
    weight_decay: 0.0
    epsilon: 1.0e-08
lr_scheduler:
  scheduler: cosine
  params:
    learning_rate: ${optimizer.params.learning_rate}
    warmup_steps: 100
    min_lr_scale: 0.1
config: configs/sft_llada_x0pred_nothink.yaml
